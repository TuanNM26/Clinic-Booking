<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>New Appointment Notification</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #ddd;
    }
    h2 {
      color: #28a745;
    }
    p {
      margin-bottom: 10px;
    }
    .appointment-details {
      margin-top: 20px;
      padding: 15px;
      border: 1px solid #eee;
      background-color: #f9f9f9;
    }
    .appointment-details strong {
      font-weight: bold;
    }
    .button-container {
      margin-top: 20px;
    }
    .btn {
      display: inline-block;
      padding: 10px 20px;
      margin-right: 10px;
      font-size: 14px;
      text-decoration: none;
      border-radius: 5px;
      color: #fff;
    }
    .btn-approve {
      background-color: #28a745;
    }
    .btn-cancel {
      background-color: #dc3545;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>New Appointment Notification</h2>
    <p>Hello Dr. {{ doctorName }},</p>
    <p>You have a new medical appointment scheduled:</p>
    <div class="appointment-details">
      <p><strong>Patient:</strong> {{ patientName }}</p>
      <p><strong>Time:</strong> {{ appointmentTime }}</p>
      <p><strong>Date:</strong> {{ appointmentDate }}</p>
      {{#if appointmentNote}}
      <p><strong>Patient's Note:</strong> {{ appointmentNote }}</p>
      {{/if}}
    </div>
    <p>Please review your schedule for more details.</p>

    <div class="button-container">
      <a href="{{baseUrl}}/appointments/status/confirm/{{appointmentId}}" class="btn btn-approve">Approve</a>
      <a href="{{baseUrl}}/appointments/status/cancel/{{appointmentId}}" class="btn btn-cancel">Cancel</a>
    </div>

    <p>Best regards,</p>
    <p>Clinic Appointment System</p>
  </div>
</body>
</html>
